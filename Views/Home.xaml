<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:SpellingBeebeto.ViewModels.GameElements"
             x:Class="SpellingBeebeto.Views.Home"
             Title="Home">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Text="Play!" VerticalOptions="Center" HorizontalOptions="Center" Command="{Binding PlayCommand}" WidthRequest="100" HeightRequest="100"/>
    </Grid>
</ContentPage>